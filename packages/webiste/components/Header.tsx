/** @jsx jsx */
import { jsx } from 'theme-ui';

const Header = () => {
  return (
    <header
      sx={{
        backgroundColor: 'primary',
        height: '100px',
      }}
    />
  );
};

export default Header;
